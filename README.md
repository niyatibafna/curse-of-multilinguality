# curse-of-multilinguality
We want to build metrics for evaluating the vulnerability of an embedding space to the curse of multilinguality

## Setup

Set `DATADIR` before running experiments. This repo stores its dataset and
embedding caches under:

```bash
${DATADIR}/projects/curse-of-multilinguality
```

For example:

```bash
export DATADIR=/path/to/data
```
